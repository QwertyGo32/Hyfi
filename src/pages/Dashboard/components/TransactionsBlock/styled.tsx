import styled from 'styled-components';

export const StyledDashboardBlockTransactions = styled.p`
  font-family: Montserrat, serif;
  font-weight: 500;
  font-size: 12px;
  line-height: 15px;
  color: var(--theme-dark-blue);
  margin-bottom: 0;
`;
